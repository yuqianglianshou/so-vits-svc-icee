# 变更记录

本项目的主要变化记录在此。版本发布遵循明确的验收结果，不为尚未验证的环境或功能作承诺。

## 未发布

### 新增

- 工作区式可视化训练页面。
- 可视化推理、模型导入、质量预设和输出管理。
- 训练依赖状态检查、自动获取和手动导入引导。
- 单模型工作区的数据、配置、日志和训练产物组织方式。
- 发布前仓库检查与轻量自动化测试。

### 改进

- 重构中英文项目首页和文档导航。
- 增加快速开始、环境兼容性、模型依赖和已知限制说明。
- 将训练和推理主入口收敛为 `src.app_train` 与 `src.app_infer`。

### 兼容性

- 保持 so-vits-svc 4.1 模型核心和文件格式兼容路线。
- 本次发布稳定化不修改模型结构、默认训练参数或 checkpoint 格式。
